default_view:
  view: yes
  icon: mdi:home 
  entities:
    - group.hall
    - group.kitchen
    - group.cabinet
    - group.bedroom
    - group.hallway
    - group.device

hall:
  name: Зал 
  entities:

kitchen:
  name: Кухня
  entities:

cabinet:
  name: Кабинет
  entities:

bedroom:
  name: Спальня
  entities:

hallway:
  name: Прихожая
  entities:

device:
  name: Контроллер
  entities:
    - sensor.cpu_temp
    - sensor.cpu_used
    - sensor.disk_used_
    - sensor.ram_available
    - sensor.processor_use
    - sensor.disk_use_percent_
    - sensor.memory_free
